[pytest]
testpaths = tests
addopts = -ra
markers =
    live: test that creates a REAL Daytona sandbox and/or calls the REAL OpenAI API.
# Third-party (pydantic/llama-index) deprecation noise is not actionable here.
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
